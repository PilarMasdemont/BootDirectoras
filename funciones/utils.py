# funciones/utils.py

def cargar_hoja(nombre_hoja: str):
    """Función provisional que simula la carga de una hoja."""
    return f"📄 Cargando hoja simulada: {nombre_hoja}"

def formatear_porcentaje(valor: float) -> str:
    """Formatea un número decimal como porcentaje con un decimal de precisión."""
    return f"{valor:.1f}%"
